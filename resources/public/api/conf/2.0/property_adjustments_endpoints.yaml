get:
  $ref: "retrieve_property_adjustments.yaml"
  summary: Retrieve a UK Property Business' Summary Adjustments
  security:
    - User-Restricted:
        - read:self-assessment

post:
  $ref: "submit_property_adjustments.yaml"
  summary: Submit an Adjustment to a UK Property Business Summary
  security:
    - User-Restricted:
        - write:self-assessment
