{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Submit a Self-Employment Business' Accounting Adjustments",
  "description": "This endpoint allows the user to provide accounting adjustments against a specified Business Source Adjustable Summary quoting its unique identifier. The BSAS ID quoted must be for a Self-Employment Business and it must not have been adjusted, previously.",
  "type": "object",
  "properties": {
    "income": { "$ref": "adjustment_definitions.json#/definitions/seIncome"},
    "expenses": { "$ref": "adjustment_definitions.json#/definitions/seExpenses"},
    "additions": { "$ref": "adjustment_definitions.json#/definitions/seAdditions"},
    "links": {
      "type": "array",
      "items": {
        "type": "object",
        "description": "A list of endpoint links that indicate possible actions related to the current resource.",
        "properties": {
          "href": {
            "description": "The relative url of the endpoint.",
            "type": "string",
            "example": "/individuals/self-assessment/adjustable-summary/AA123456A"
          },
          "rel": {
            "description": "A label for the endpoint, which describes how it is related to the current resource. The rel will be self where the action is retrieval of the same resource.",
            "type": "string",
            "enum": [
              "self", "retrieve-adjustable-summary"
            ]
          },
          "method": {
            "description": "The HTTP method type for the endpoint.",
            "type": "string",
            "enum": [
              "GET"
            ]
          }
        },
        "required": [
          "href",
          "rel",
          "method"
        ]
      }
    }
  }
}